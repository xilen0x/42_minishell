#include "minishell.h"

size_t tok_word_cnt(t_lst *tokens)
{
    t_lst           *aux;
    unsigned int    cnt;

    aux = tokens;
    cnt = 0;
    while (aux)
    {
        if (aux->key == WORD)
            cnt++;
        aux = aux->next;
    }
    return (cnt);
}